- name: Debug remote environment
        env:
          STB_HOST: ${{ secrets.STB_HOST }}
        run: |
          ssh -F $HOME/.ssh/config "$STB_HOST" '
            whoami
            echo "HOME=$HOME"
            echo "PATH=$PATH"
            ls -la $HOME/.bun/bin 2>&1
            export BUN_INSTALL="$HOME/.bun"
            export PATH="$BUN_INSTALL/bin:$PATH"
            echo "PATH after export=$PATH"
            which bun
          '